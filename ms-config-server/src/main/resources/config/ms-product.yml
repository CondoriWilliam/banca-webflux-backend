spring:
  application:
    name: ms-product
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/msvc-product-wbf
    username: postgres #${DATABASE_USERNAME}
    password: 1234 #${DATABASE_PASSWORD}

server:
  port: 8083

#logging:
#  level:
#    org.springframework.r2dbc: DEBUG
#    io.r2dbc.postgresql.QUERY: DEBUG
#    io.r2dbc.postgresql.PARAM: DEBUG

eureka:
  instance:
    hostname: msvc-discovery-server
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka #http://msvc-discovery-server:8761/eureka


